---
import {
    DisplaySettingsFragment,
    ImageComponentFragment,
} from '../../../../../services/graphql/__generated/sdk';
import { getImageComponentStyles } from './ImageStyling';
import { ContentPayload } from '../../../../../services/shared/ContentPayload';

export interface Props {
    data: ImageComponentFragment;
    displaySettings: DisplaySettingsFragment[];
    contentPayload: ContentPayload;
}
const { data, displaySettings } = Astro.props as Props;
---

<img
    class:list={getImageComponentStyles(displaySettings)}
    src={data.imageLink.url.default}
    alt={data.altText}
/>
